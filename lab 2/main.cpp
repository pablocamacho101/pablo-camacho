/* kattis - triangle area problem
function and unit testing
cscI
*/

#include <iostream>
#include  <cstdio>
#include <cassert>
#include <string>

float areaOfTriangle(const unsigned height, const unsigned base);
void testArea();

# define Max_ERROR 1e-7
int main()
{
    testArea(double height, double base);
    unsigned height, base;
    float answer;
    cin>>height>>base;
    //Fix me 1 Call area function passing proper arguments
	// store the returned result into answer variable
    double result = height*base/2.0;


    answer=0;
    printf("%.7f\n", answer):
    return 0;
    //function implementation
    float areaOfTraingle(const unsigned height, const unsigned base)
    float area=0;
     // FIXME2: Find the area of traingle using the formular given in algorithm step: 2.a
  // store the area into area variable
    //fix me

    void testArea()
    {
        void testArea()
        {
            unsigned hegiht, base;
            height=10;
            base=5;
            answer= areaOfTraingle(height, base);
            expected= 25.0;
            asser(abs(answer-expected)<Max_ERROR)
            // fix 3  2. define a function to find an return area given height and base of a right triangle
            cout << result
            //fix 4  3. Call the function to print the result
            //fix 5  4. Write a test function to test the area function implemented in step 2



        }

    }


}